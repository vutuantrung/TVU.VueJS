NOTE v-model, if used on an input of type number automatically fetches the user input and converts it from a string to a number of data type.
By default, if you would be using just JavaScript or if you're using refs and therefore the native JavaScript object representing this input.By default, what's stored in the value is always a string.